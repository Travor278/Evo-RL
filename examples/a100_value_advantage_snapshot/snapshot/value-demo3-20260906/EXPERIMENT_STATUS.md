# Three-task value experiment handoff

## CURRENT PRIORITY OVERRIDE — fixed-horizon ablations (2026-09-06)

The user explicitly stopped the old8k/early-stop pilot to prioritize five architectures and fixed step budgets. Its last complete checkpoint006000 is preserved; value-demo3-pipeline was intentionally interrupted via its exact torchrun PID, and all its GPU processes exited. Do NOT resume this old queue or automatically launch its fold/stationery stages. The pending extended review below was not executed and is preserved for follow-up.

Read /data/experiments/value-ablation-20260906/EXPERIMENT_STATUS.md and PROTOCOL.md instead. Active tmux=value-ablation-pipeline. Five variants are full, vision_only (no task/state/Gemma and no state MLP), frozen_language, frozen_both, task_only (task but no state). New runs stop at exact fixed horizons;1500 screen and fresh2500/3000 candidates. Monitor id=value has been updated. No dataset, checkpoint or upstream source was deleted/modified for this switch.

## Latest: user-authorized post-hoc diagnostics (2026-09-06 ~14:10 China)

- User explicitly requested actual comprehensive metric/code analysis, not more theoretical discussion. No retraining is needed. Existing formal pipeline stays running; no concurrent GPU job was launched.
- CPU audit DONE for PiperX checkpoints250..5500 using all saved 56x32 holdout predictions. Results: `reports/posthoc-20260906/{saved_prediction_audit.json,checkpoint_metrics.csv,episode_metrics.csv,paired_comparisons.csv,report.md}`. Code `reports/posthoc-20260906/analyze_saved.py`; local plots/report mirrored under the calling task visualization directory `value-diagnostics-20260906`.
- Original macro-MAE best/early-stop protocol unchanged. Observed at this snapshot: CEbest1500, MAEbest4750, RMSEbest5500. Paired whole-episode bootstrap(10000/seed1000): 3000 minus1500 MAE=-0.00117870,95%CI[-0.00209242,-0.00024815],39/56 improve; 4750 minus3000=-0.00008135,CI[-0.00081920,0.00066160],25/56 improve. This is conditional post-hoc analysis, not selection-adjusted significance; episode/session independence is unverified. Do not declare a global optimum.
- Metrics computed: train/holdout CE, MAE, RMSE; bias/P95/timing conversion; initial/terminal and four temporal quarters; per-episode Spearman/Kendall vs time-derived labels; sparse-grid reverse fraction and delta error; paired bootstrap. Sparse deltas are variable gaps, NOT true50-frame advantages; ordering is not independent physical-progress ground truth.
- NEW inference-only comparison is installed and QUEUED at the existing dense-review stage. `extended-review-request.json` enables `extended_value_review.py` via five lines ONLY in standalone runner's `--evaluate-checkpoint` branch. Original standalone file backed up at `code/value_experiment.pre-extended-review-v1.py`; upstream LeRobot/Evo sources and training branch untouched. Do not relaunch pipeline/training to activate it; next normal dense invocation picks it up.
- PiperX candidates1500/3000/4750/5500 plus observed CE/MAE/RMSE minima at stage end, deduplicated/frozen in `runs/RUN/extended-review-v1/plan.json`. Other tasks use their own observed three metric minima. After existing best128 review: strict reload each candidate, no_grad/model.eval BF16 inference, test128 fixed base frames and train32 matched-cohort frames per episode, plus16 exact t,t+50 pairs in BOTH splits. No optimizer or gradients. This adds inference time but never runs alongside training.
- Extended outputs: `runs/RUN/extended-review-v1/checkpoint-N/{summary.json,train-distributions.npz,test-distributions.npz,train-pairs50.npy,test-pairs50.npy}` and final `COMPLETE.json`. Logs in existing `RUN-dense.log` with EXTENDED_REVIEW_START/PROGRESS/CHECKPOINT_COMPLETE/COMPLETE. Already-completed candidates safely skipped. Tests for distribution expectation/CE, quantiles, CRPS, serialization,exact50 pairs PASSED locally and in target environment; Python compilation passed. Actual GPU execution of this NEW extended evaluator remains pending until the pipeline reaches the boundary; do not claim it already passed.
- Extended metrics: CE/MAE/RMSE, entropy, predicted spread, ordinalCRPS,90% projected-return interval coverage/width, quantile observedCDF; exact50-frame residual errors; TRAIN-fitted top30% residual threshold and holdout label agreement/Jaccard across checkpoints. These label metrics indicate stability, NOT correctness. Current success-countdown target has identically zero realized Bellman residual, so never call zero residual or lower residual proof of useful advantage. No failures/preferences or policy-rollout evaluation can be fabricated.
- Monitor must now verify EXTENDED_REVIEW_COMPLETE/status=ok before describing each task's review as complete. If this new analysis fails, preserve outputs and diagnose; no repeated environment installs/second GPU jobs. Training pipeline remains automatic task1→analysis→task2→analysis→task3→analysis. Retain original selection while reporting post-hoc candidates separately.

Experiment root: `/data/experiments/value-demo3-20260906`.
Updated 2026-09-06 during formal PiperX startup verification.

## Completed

- Three independent, fixed seed1000 whole-episode 90/10 manifests in `split90_10/manifests`; numeric audit all OK. Only train-fitted quantiles in `split90_10/train-only-stats`.
- Pinned SigLIP and Gemma backbone snapshots downloaded; `backbones-download.log` ends `ALL_BACKBONES_COMPLETE`.
- Official Evo-RL reference copied to `code/Evo-RL-reference`, upstream commit `6f2db449a21e1bac750b996f2e27cac6739aa63f`. Do not install this old0.4.4 fork over official0.6.1.
- Standalone `value_experiment.py` created; imports original architecture without changing upstream files. Import-only compatibility alias for moved `processor.core` types to `lerobot_types`, subclass handles Transformers5 structured image feature output.
- Real PyAV first/middle/last frame decode for one train episode of each task passed; all cameras shape3x3x480x640, tolerance1e-4. Stateless sampler resume test passed.

## Active

- Exact env installation COMPLETE. Python3.12, torch2.10.0+cu128, torchvision0.25.0+cu128, CUDA12.8, officialLeRobot0.6.1, transformers5.3.0, accelerate1.13.0, datasets4.8.5, av15.1.0 verified. Import/GPU checks and `pip check` pass. Do not reinstall.
- Temporary single-GPU compatibility smoke in existing OLD env (torch2.11/transformers5.5) COMPLETED: log `compatibility-smoke-oldenv-v1.log` ends `SMOKE_SAVE_RELOAD_OK` and `TRAINING_STAGE_COMPLETE`; 2 updates, losses5.3685/5.4079, peak15.81GB, actual eval56heldout+56trainframes and exact logits reproduction after checkpoint reload. This is NOT the final required target-environment smoke and NOT formal training. Preserve output.
- Old-env **8GPU** compatibility smoke also COMPLETED: `compatibility-smoke-oldenv-8gpu-v1.log`, 3 updates globalbatch32, loss5.34864/5.32508/5.24305, grdn7.8395/8.2128/6.1882, lr1.67e-5/3.33e-5/5e-5, peak20.50GB, steady3.39seconds/step. Distributed eval168test+168trainframes, checkpoint/save/reload equality all passed. Tiny3step model is deliberately untrained and worse than constant baseline; not a convergence result.
- Initial targetenv smoke v1 found a rank race in protocol creation before the first update (late rank mistook rank0's newly created protocol for an old experiment). Fixed solely in standalone runner: rank0 broadcasts preexisting-protocol flag before writing, then barrier. Old code saved under `code/value_experiment.pre-protocol-race-fix.py`, failedsmoke output/log preserved.
- Required **targetenv8GPU smoke v2 PASSED**: `targetenv-8gpu-smoke-v2.log`, three updates, real distributed eval, strict saved-weight reload reproduces logits exactly, peak20.50GB. Last training loss5.2444, testCE5.1082/MAE0.39755 are tiny-smoke values, NOT converged results.
- **Formal PiperX training STARTED and verified through step20**, pipeline tmux `value-demo3-pipeline`; log `piperx_insert_copper_screw-bf16-gb32-seed1000-pilot8k-v1.log`. step10loss5.37434/grdn9.13554/lr2.5e-6; step20loss5.25089/grdn8.13171/lr5e-6/effective_epoch0.000565. All8GPU100%,21,381MiB each; steady3.43seconds/step. It has1,132,703trainframes,56heldoutepisodes/1,792fixed evalframes; first formaleval atstep250. Pipeline automatically continues dense best review -> fold -> stationery. Do not launch another GPU job or restart the whole pipeline while active.
- Derived report helper `report_value_experiments.py` installed/tested (currently no completed formaleval). Run it during later monitors to update `reports/summary.json`, `reports/summary.md`, per-runSVG loss/MAE curves. It excludes smoke runs. Dense-best outputs optionally provide episode-bootstrapMAE95% intervals, conditional on selected checkpoint, not selection-adjusted confidence.
- Metadata audit: no explicit capture-session/date IDs found in any selected task's episode schema. PiperX task_info has instruction/label/scene only; action_config is a list. Existing exact trajectory hashes rule out identical episodes, but shared-session/near-duplicate visual leakage remains unverified. Keep this limitation in final report.

## Runner protocol

`--task TASK --run UNIQUE_NAME --steps 8000 --batch-size 4 --workers 2 --eval-every 250 --eval-frames 32 --warmup 200 --min-steps 2000 --patience 8`.
Use target env torchrun8 after actual8GPU smoke succeeds. Model weights FP32, AMP BF16, gradient checkpointing enabled; all used backbone parameters trainable; nativeDDP find_unused_parameters for unused SigLIP text branch. No FSDP, no expert-only policy changes.

Reward exactly successful Evo MC `-(length-frame-1)/(2*max_train_length)`, retains upstream c_fail_coef1 successful scale with no failure data/penalty applied. Train maxima7441/17943/8968, test5753/17047/5351, so no test clipping/support violation. State uses train q01/q99, original digitize prompt/pad32, images original model resize+normalization. Loss original201bin two-neighbor interpolation CE.

Holdout evaluation fixed32 evenly spaced frames EACH episode, both endpoints; train-eval matches holdout episode count chosen deterministically from train. This is explicitly subsampled, not whole-frame test. Select minimum macro episode MAE; patience counts evals without0.5% relative improvement after2000steps. Preserve immutable checkpoint-step.pt + best/last JSON pointers at each250eval. Fixed LR cosine5e-5 to1e-6 over8000steps. Training sampler uniform frames WITH replacement: effective_epoch counts draws, not guaranteed distinct coverage. Every step random sampling is deterministic given seed/step/world, supporting exact data resume. Resume requires unchanged manifests/stats/globalbatch/schedule.

## Still required

1. Monitor formal PiperX step/eval progress andGPU/log errors; do not repeat completed environment or smoke work.
2. If genuine failure, inspect/resume last immutable checkpoint while preserving best pointer/history. torchrun requires `--` BEFORE `value_experiment.py`, otherwise `--run` is misparsed as launcher `--run-path`; this is fixed in launcher. DataLoader uses a dedicated RNG generator so worker initialization does not perturb model/dropout RNG on resume.
3. Standalone dense best evaluation mode implemented, still needs runtime test: `--task TASK --run RUN --evaluate-checkpoint best --eval-frames 128`, or explicit checkpoint filename. No optimizer/updates; writes dense-review and dense-predictions JSON. Add final checkpoint export and curves/report generation. Current runner writes stage_complete with dense_best_review_required=true, intentionally not final success.
4. Pipeline handles PiperX pilot/formal after target smoke, then fold and stationery automatically, with128frames/heldoutepisode dense best review after each. Names: `TASK-bf16-gb32-seed1000-pilot8k-v1`; logs in experiment root, outputs in `runs/`. It is fail-closed and never overwrites existing output directories; if it exits on a real failure inspect before safe targeted recovery. Do not wait for new user approval or duplicate full8GPU runs.
5. Inspect test-curve plateau and baseline improvement, do not claim optimum when best is at budget boundary. Additional training phases must record any changed scheduler as a new experiment phase. Full desired result is three best-step recommendations with uncertainty, not merely finishing8000steps.

User sleeping; routine unchanged progress quiet. Notify only meaningful results/errors needing attention. No source data, old checkpoints, current policy env, or unrelated workflows modified.
